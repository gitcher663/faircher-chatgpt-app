{
  "name": "faircher-chatgpt-app",
  "private": true,
  "scripts": {
    "build": "npm --prefix ui install && npm --prefix server install && npm --prefix ui run build && npm --prefix server run build",
    "start": "cd server && npm run start"
  }
}
