{
  "name": "faircher-chatgpt-app",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "cd server && npm run start"
  }
}
